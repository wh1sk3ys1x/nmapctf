# Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Jinja2/HTMX/Bootstrap 5 web UI for nmapctf — dashboard, asset management, scan profiles, schedules, scan execution, scan history, and scan detail pages.

**Architecture:** View route handlers in `web/app/views/` render Jinja2 templates from `web/app/templates/`. Views query the database directly (same FastAPI app as the API). HTMX handles dynamic interactions (delete confirmations, form submissions, scan status polling). All pages share a base layout with Bootstrap 5 navbar.

**Tech Stack:** Jinja2, HTMX 2.x (CDN), Bootstrap 5.3 (CDN), FastAPI (Jinja2Templates + HTMLResponse)

---

## File Structure

**New directories:**
- `web/app/templates/` — Jinja2 templates
- `web/app/templates/partials/` — HTMX partial templates
- `web/app/views/` — View route handlers
- `web/app/static/` — Custom CSS

**New files:**

| File | Purpose |
|------|---------|
| `web/app/views/__init__.py` | Empty init |
| `web/app/views/dashboard.py` | Dashboard page route |
| `web/app/views/assets.py` | Asset CRUD view routes |
| `web/app/views/profiles.py` | Profile CRUD view routes |
| `web/app/views/schedules.py` | Schedule CRUD view routes |
| `web/app/views/scans.py` | Run scan, scan history, scan detail view routes |
| `web/app/templates/base.html` | Shared layout: Bootstrap 5, HTMX, navbar, flash messages |
| `web/app/templates/dashboard.html` | Dashboard with stats, recent scans, upcoming schedules |
| `web/app/templates/assets/list.html` | Assets table with add/edit/delete |
| `web/app/templates/assets/form.html` | Asset create/edit form |
| `web/app/templates/profiles/list.html` | Profiles table |
| `web/app/templates/profiles/form.html` | Profile create/edit form |
| `web/app/templates/schedules/list.html` | Schedules table with enable/disable toggle |
| `web/app/templates/schedules/form.html` | Schedule create/edit form |
| `web/app/templates/scans/history.html` | Filterable scan history list |
| `web/app/templates/scans/detail.html` | Scan detail with results table + raw XML |
| `web/app/templates/scans/run.html` | Run scan form (pick asset + profile) |
| `web/app/templates/partials/scan_status.html` | HTMX partial for polling scan status |
| `web/app/templates/partials/flash.html` | Flash message partial |
| `web/app/static/style.css` | Minimal custom styles |
| `tests/web/test_views.py` | Functional tests for view routes |

**Modified files:**

| File | Change |
|------|--------|
| `web/app/main.py` | Mount templates, static files, include view routers |

---

### Task 1: Base Layout and Static Files

Set up the shared template, static CSS, and wire Jinja2Templates + StaticFiles into the FastAPI app.

**Files:**
- Create: `web/app/templates/base.html`
- Create: `web/app/templates/partials/flash.html`
- Create: `web/app/static/style.css`
- Create: `web/app/views/__init__.py`
- Modify: `web/app/main.py`

- [ ] **Step 1: Create the static CSS file**

```css
/* web/app/static/style.css */
body { padding-top: 56px; }
.table td, .table th { vertical-align: middle; }
.scan-status-pending { color: #6c757d; }
.scan-status-running { color: #0d6efd; }
.scan-status-completed { color: #198754; }
.scan-status-failed { color: #dc3545; }
pre.raw-xml { max-height: 500px; overflow-y: auto; background: #f8f9fa; padding: 1rem; border-radius: 0.375rem; }
```

- [ ] **Step 2: Create the flash message partial**

```html
{# web/app/templates/partials/flash.html #}
{% with messages = get_flashed_messages() %}
{% if messages %}
{% for category, message in messages %}
<div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
  {{ message }}
  <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
</div>
{% endfor %}
{% endif %}
{% endwith %}
```

Note: We'll implement flash messages using a simple cookie-based approach since FastAPI doesn't have built-in flash support. We'll add a `flash()` helper and Jinja2 context processor in the main.py modifications.

- [ ] **Step 3: Create the base layout template**

```html
{# web/app/templates/base.html #}
<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}nmapctf{% endblock %}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/static/style.css" rel="stylesheet">
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">nmapctf</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link" href="/">Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/assets">Assets</a></li>
          <li class="nav-item"><a class="nav-link" href="/profiles">Profiles</a></li>
          <li class="nav-item"><a class="nav-link" href="/schedules">Schedules</a></li>
          <li class="nav-item"><a class="nav-link" href="/scans">Scan History</a></li>
          <li class="nav-item"><a class="nav-link" href="/scans/run">Run Scan</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container mt-4">
    {% include "partials/flash.html" %}
    {% block content %}{% endblock %}
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

- [ ] **Step 4: Create empty views init**

```python
# web/app/views/__init__.py
```

- [ ] **Step 5: Update main.py to mount templates, static files, and add flash helper**

Add to `web/app/main.py`:

```python
from contextlib import asynccontextmanager
import json

from fastapi import FastAPI, Request, Response
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates
from pathlib import Path

from app.database import Base, engine, SessionLocal
from app.seed import seed_default_profiles
from app.api import assets, profiles, scans, schedules, internal


@asynccontextmanager
async def lifespan(app: FastAPI):
    Base.metadata.create_all(bind=engine)
    db = SessionLocal()
    try:
        seed_default_profiles(db)
    finally:
        db.close()
    yield


app = FastAPI(title="nmapctf", version="0.1.0", lifespan=lifespan)

# Static files and templates
BASE_DIR = Path(__file__).resolve().parent
app.mount("/static", StaticFiles(directory=BASE_DIR / "static"), name="static")
templates = Jinja2Templates(directory=BASE_DIR / "templates")


# Flash message helpers (cookie-based)
def flash(response: Response, message: str, category: str = "info"):
    """Store a flash message in a cookie."""
    existing = response.headers.get("set-cookie", "")
    # Use a simple JSON list in a cookie
    messages = [[category, message]]
    response.set_cookie("flash_messages", json.dumps(messages), max_age=5)


def get_flashed_messages(request: Request, response: Response):
    """Retrieve and clear flash messages from cookies."""
    raw = request.cookies.get("flash_messages")
    if raw:
        response.delete_cookie("flash_messages")
        try:
            return json.loads(raw)
        except json.JSONDecodeError:
            return []
    return []


# Make flash helper available in all templates
@app.middleware("http")
async def flash_middleware(request: Request, call_next):
    response = await call_next(request)
    return response


# API routers
app.include_router(assets.router, prefix="/api/v1")
app.include_router(profiles.router, prefix="/api/v1")
app.include_router(scans.router, prefix="/api/v1")
app.include_router(schedules.router, prefix="/api/v1")
app.include_router(internal.router, prefix="/api/v1")


@app.get("/api/v1/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 6: Commit**

```bash
git add web/app/templates/base.html web/app/templates/partials/flash.html web/app/static/style.css web/app/views/__init__.py web/app/main.py
git commit -m "Add base layout template, static files, and Jinja2/HTMX setup"
```

---

### Task 2: Dashboard Page

**Files:**
- Create: `web/app/views/dashboard.py`
- Create: `web/app/templates/dashboard.html`
- Modify: `web/app/main.py` (include dashboard router)

- [ ] **Step 1: Create the dashboard view**

```python
# web/app/views/dashboard.py
from fastapi import APIRouter, Request
from fastapi.responses import HTMLResponse
from sqlalchemy import func

from app.api.deps import DbSession
from app.models import ScanJob, Asset, Schedule, ScanStatus

router = APIRouter(tags=["views"])


@router.get("/", response_class=HTMLResponse)
def dashboard(request: Request, db: DbSession):
    from app.main import templates

    total_assets = db.query(func.count(Asset.id)).scalar()
    total_scans = db.query(func.count(ScanJob.id)).scalar()
    running_scans = db.query(func.count(ScanJob.id)).filter(
        ScanJob.status.in_([ScanStatus.pending, ScanStatus.running])
    ).scalar()
    active_schedules = db.query(func.count(Schedule.id)).filter(
        Schedule.enabled == True
    ).scalar()

    recent_scans = (
        db.query(ScanJob)
        .order_by(ScanJob.queued_at.desc())
        .limit(10)
        .all()
    )

    upcoming_schedules = (
        db.query(Schedule)
        .filter(Schedule.enabled == True)
        .order_by(Schedule.name)
        .limit(5)
        .all()
    )

    return templates.TemplateResponse(
        "dashboard.html",
        {
            "request": request,
            "total_assets": total_assets,
            "total_scans": total_scans,
            "running_scans": running_scans,
            "active_schedules": active_schedules,
            "recent_scans": recent_scans,
            "upcoming_schedules": upcoming_schedules,
        },
    )
```

- [ ] **Step 2: Create the dashboard template**

```html
{# web/app/templates/dashboard.html #}
{% extends "base.html" %}
{% block title %}Dashboard — nmapctf{% endblock %}
{% block content %}
<h1 class="mb-4">Dashboard</h1>

<div class="row g-3 mb-4">
  <div class="col-md-3">
    <div class="card text-center">
      <div class="card-body">
        <h5 class="card-title text-muted">Assets</h5>
        <p class="display-6">{{ total_assets }}</p>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card text-center">
      <div class="card-body">
        <h5 class="card-title text-muted">Total Scans</h5>
        <p class="display-6">{{ total_scans }}</p>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card text-center">
      <div class="card-body">
        <h5 class="card-title text-muted">Active Scans</h5>
        <p class="display-6">{{ running_scans }}</p>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card text-center">
      <div class="card-body">
        <h5 class="card-title text-muted">Schedules</h5>
        <p class="display-6">{{ active_schedules }}</p>
      </div>
    </div>
  </div>
</div>

<div class="row g-3">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span>Recent Scans</span>
        <a href="/scans" class="btn btn-sm btn-outline-light">View All</a>
      </div>
      <div class="card-body p-0">
        <table class="table table-hover mb-0">
          <thead>
            <tr>
              <th>Asset</th>
              <th>Profile</th>
              <th>Status</th>
              <th>Queued</th>
            </tr>
          </thead>
          <tbody>
            {% for scan in recent_scans %}
            <tr>
              <td><a href="/assets/{{ scan.asset_id }}">{{ scan.asset.name }}</a></td>
              <td>{{ scan.profile.name }}</td>
              <td><span class="scan-status-{{ scan.status.value }}">{{ scan.status.value }}</span></td>
              <td>{{ scan.queued_at.strftime('%Y-%m-%d %H:%M') }}</td>
            </tr>
            {% else %}
            <tr><td colspan="4" class="text-muted text-center">No scans yet. <a href="/scans/run">Run one now.</a></td></tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span>Active Schedules</span>
        <a href="/schedules" class="btn btn-sm btn-outline-light">Manage</a>
      </div>
      <ul class="list-group list-group-flush">
        {% for sched in upcoming_schedules %}
        <li class="list-group-item d-flex justify-content-between">
          <span>{{ sched.name }}</span>
          <code>{{ sched.cron_expression }}</code>
        </li>
        {% else %}
        <li class="list-group-item text-muted text-center">No active schedules.</li>
        {% endfor %}
      </ul>
    </div>
  </div>
</div>
{% endblock %}
```

- [ ] **Step 3: Register dashboard router in main.py**

Add to the imports and router registrations in `web/app/main.py`:

```python
from app.views import dashboard as dashboard_views

# After API routers:
app.include_router(dashboard_views.router)
```

- [ ] **Step 4: Commit**

```bash
git add web/app/views/dashboard.py web/app/templates/dashboard.html web/app/main.py
git commit -m "Add dashboard page with stats and recent scans"
```

---

### Task 3: Assets Pages (List + Create/Edit + Delete)

**Files:**
- Create: `web/app/views/assets.py`
- Create: `web/app/templates/assets/list.html`
- Create: `web/app/templates/assets/form.html`
- Modify: `web/app/main.py` (include router)

- [ ] **Step 1: Create the assets view routes**

```python
# web/app/views/assets.py
from fastapi import APIRouter, Request, Form
from fastapi.responses import HTMLResponse, RedirectResponse

from app.api.deps import DbSession
from app.models import Asset, AssetType

router = APIRouter(prefix="/assets", tags=["views"])


@router.get("/", response_class=HTMLResponse)
def list_assets(request: Request, db: DbSession):
    from app.main import templates
    assets = db.query(Asset).order_by(Asset.name).all()
    return templates.TemplateResponse("assets/list.html", {"request": request, "assets": assets})


@router.get("/new", response_class=HTMLResponse)
def new_asset(request: Request):
    from app.main import templates
    return templates.TemplateResponse(
        "assets/form.html",
        {"request": request, "asset": None, "asset_types": list(AssetType)},
    )


@router.get("/{asset_id}/edit", response_class=HTMLResponse)
def edit_asset(asset_id: int, request: Request, db: DbSession):
    from app.main import templates
    asset = db.get(Asset, asset_id)
    if not asset:
        return RedirectResponse("/assets", status_code=303)
    return templates.TemplateResponse(
        "assets/form.html",
        {"request": request, "asset": asset, "asset_types": list(AssetType)},
    )


@router.post("/", response_class=HTMLResponse)
def create_asset(
    db: DbSession,
    name: str = Form(...),
    type: str = Form(...),
    address: str = Form(...),
    notes: str = Form(""),
):
    asset = Asset(name=name, type=AssetType(type), address=address, notes=notes or None)
    db.add(asset)
    db.commit()
    return RedirectResponse("/assets", status_code=303)


@router.post("/{asset_id}", response_class=HTMLResponse)
def update_asset(
    asset_id: int,
    db: DbSession,
    name: str = Form(...),
    type: str = Form(...),
    address: str = Form(...),
    notes: str = Form(""),
):
    asset = db.get(Asset, asset_id)
    if not asset:
        return RedirectResponse("/assets", status_code=303)
    asset.name = name
    asset.type = AssetType(type)
    asset.address = address
    asset.notes = notes or None
    db.commit()
    return RedirectResponse("/assets", status_code=303)


@router.delete("/{asset_id}")
def delete_asset(asset_id: int, db: DbSession):
    asset = db.get(Asset, asset_id)
    if asset:
        db.delete(asset)
        db.commit()
    return HTMLResponse("")
```

- [ ] **Step 2: Create the assets list template**

```html
{# web/app/templates/assets/list.html #}
{% extends "base.html" %}
{% block title %}Assets — nmapctf{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Assets</h1>
  <a href="/assets/new" class="btn btn-primary">Add Asset</a>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Address</th>
      <th>Notes</th>
      <th style="width: 120px;">Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for asset in assets %}
    <tr id="asset-{{ asset.id }}">
      <td>{{ asset.name }}</td>
      <td><span class="badge bg-secondary">{{ asset.type.value }}</span></td>
      <td><code>{{ asset.address }}</code></td>
      <td>{{ asset.notes or '' }}</td>
      <td>
        <a href="/assets/{{ asset.id }}/edit" class="btn btn-sm btn-outline-light">Edit</a>
        <button class="btn btn-sm btn-outline-danger"
                hx-delete="/assets/{{ asset.id }}"
                hx-target="#asset-{{ asset.id }}"
                hx-swap="outerHTML"
                hx-confirm="Delete asset '{{ asset.name }}'?">Del</button>
      </td>
    </tr>
    {% else %}
    <tr><td colspan="5" class="text-muted text-center">No assets yet. <a href="/assets/new">Add one.</a></td></tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 3: Create the asset form template**

```html
{# web/app/templates/assets/form.html #}
{% extends "base.html" %}
{% block title %}{{ 'Edit' if asset else 'New' }} Asset — nmapctf{% endblock %}
{% block content %}
<h1>{{ 'Edit' if asset else 'New' }} Asset</h1>

<form method="post" action="{{ '/assets/' ~ asset.id if asset else '/assets/' }}" class="mt-3" style="max-width: 600px;">
  <div class="mb-3">
    <label for="name" class="form-label">Name</label>
    <input type="text" class="form-control" id="name" name="name" required
           value="{{ asset.name if asset else '' }}">
  </div>
  <div class="mb-3">
    <label for="type" class="form-label">Type</label>
    <select class="form-select" id="type" name="type" required>
      {% for t in asset_types %}
      <option value="{{ t.value }}" {{ 'selected' if asset and asset.type == t else '' }}>{{ t.value }}</option>
      {% endfor %}
    </select>
  </div>
  <div class="mb-3">
    <label for="address" class="form-label">Address</label>
    <input type="text" class="form-control" id="address" name="address" required
           placeholder="IP, hostname, CIDR, or range"
           value="{{ asset.address if asset else '' }}">
  </div>
  <div class="mb-3">
    <label for="notes" class="form-label">Notes</label>
    <textarea class="form-control" id="notes" name="notes" rows="3">{{ asset.notes if asset and asset.notes else '' }}</textarea>
  </div>
  <button type="submit" class="btn btn-primary">{{ 'Update' if asset else 'Create' }}</button>
  <a href="/assets" class="btn btn-outline-secondary">Cancel</a>
</form>
{% endblock %}
```

- [ ] **Step 4: Register assets view router in main.py**

```python
from app.views import assets as asset_views

app.include_router(asset_views.router)
```

- [ ] **Step 5: Commit**

```bash
git add web/app/views/assets.py web/app/templates/assets/ web/app/main.py
git commit -m "Add assets pages with list, create, edit, and delete"
```

---

### Task 4: Profiles Pages (List + Create/Edit + Delete)

**Files:**
- Create: `web/app/views/profiles.py`
- Create: `web/app/templates/profiles/list.html`
- Create: `web/app/templates/profiles/form.html`
- Modify: `web/app/main.py` (include router)

- [ ] **Step 1: Create the profiles view routes**

```python
# web/app/views/profiles.py
from fastapi import APIRouter, Request, Form
from fastapi.responses import HTMLResponse, RedirectResponse

from app.api.deps import DbSession
from app.models import ScanProfile

router = APIRouter(prefix="/profiles", tags=["views"])


@router.get("/", response_class=HTMLResponse)
def list_profiles(request: Request, db: DbSession):
    from app.main import templates
    profiles = db.query(ScanProfile).order_by(ScanProfile.name).all()
    return templates.TemplateResponse("profiles/list.html", {"request": request, "profiles": profiles})


@router.get("/new", response_class=HTMLResponse)
def new_profile(request: Request):
    from app.main import templates
    return templates.TemplateResponse("profiles/form.html", {"request": request, "profile": None})


@router.get("/{profile_id}/edit", response_class=HTMLResponse)
def edit_profile(profile_id: int, request: Request, db: DbSession):
    from app.main import templates
    profile = db.get(ScanProfile, profile_id)
    if not profile:
        return RedirectResponse("/profiles", status_code=303)
    return templates.TemplateResponse("profiles/form.html", {"request": request, "profile": profile})


@router.post("/", response_class=HTMLResponse)
def create_profile(
    db: DbSession,
    name: str = Form(...),
    nmap_args: str = Form(...),
    description: str = Form(""),
):
    profile = ScanProfile(name=name, nmap_args=nmap_args, description=description or None)
    db.add(profile)
    db.commit()
    return RedirectResponse("/profiles", status_code=303)


@router.post("/{profile_id}", response_class=HTMLResponse)
def update_profile(
    profile_id: int,
    db: DbSession,
    name: str = Form(...),
    nmap_args: str = Form(...),
    description: str = Form(""),
):
    profile = db.get(ScanProfile, profile_id)
    if not profile or profile.is_default:
        return RedirectResponse("/profiles", status_code=303)
    profile.name = name
    profile.nmap_args = nmap_args
    profile.description = description or None
    db.commit()
    return RedirectResponse("/profiles", status_code=303)


@router.delete("/{profile_id}")
def delete_profile(profile_id: int, db: DbSession):
    profile = db.get(ScanProfile, profile_id)
    if profile and not profile.is_default:
        db.delete(profile)
        db.commit()
    return HTMLResponse("")
```

- [ ] **Step 2: Create the profiles list template**

```html
{# web/app/templates/profiles/list.html #}
{% extends "base.html" %}
{% block title %}Scan Profiles — nmapctf{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Scan Profiles</h1>
  <a href="/profiles/new" class="btn btn-primary">Add Profile</a>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Arguments</th>
      <th>Description</th>
      <th style="width: 120px;">Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for profile in profiles %}
    <tr id="profile-{{ profile.id }}">
      <td>
        {{ profile.name }}
        {% if profile.is_default %}<span class="badge bg-info ms-1">default</span>{% endif %}
      </td>
      <td><code>{{ profile.nmap_args }}</code></td>
      <td>{{ profile.description or '' }}</td>
      <td>
        {% if not profile.is_default %}
        <a href="/profiles/{{ profile.id }}/edit" class="btn btn-sm btn-outline-light">Edit</a>
        <button class="btn btn-sm btn-outline-danger"
                hx-delete="/profiles/{{ profile.id }}"
                hx-target="#profile-{{ profile.id }}"
                hx-swap="outerHTML"
                hx-confirm="Delete profile '{{ profile.name }}'?">Del</button>
        {% endif %}
      </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 3: Create the profile form template**

```html
{# web/app/templates/profiles/form.html #}
{% extends "base.html" %}
{% block title %}{{ 'Edit' if profile else 'New' }} Profile — nmapctf{% endblock %}
{% block content %}
<h1>{{ 'Edit' if profile else 'New' }} Scan Profile</h1>

<form method="post" action="{{ '/profiles/' ~ profile.id if profile else '/profiles/' }}" class="mt-3" style="max-width: 600px;">
  <div class="mb-3">
    <label for="name" class="form-label">Name</label>
    <input type="text" class="form-control" id="name" name="name" required
           value="{{ profile.name if profile else '' }}">
  </div>
  <div class="mb-3">
    <label for="nmap_args" class="form-label">Nmap Arguments</label>
    <input type="text" class="form-control" id="nmap_args" name="nmap_args" required
           placeholder="-sV -sC -p 1-1000"
           value="{{ profile.nmap_args if profile else '' }}">
    <div class="form-text">Raw nmap flags, e.g. <code>-sV -sC -p 1-1000</code></div>
  </div>
  <div class="mb-3">
    <label for="description" class="form-label">Description</label>
    <textarea class="form-control" id="description" name="description" rows="3">{{ profile.description if profile and profile.description else '' }}</textarea>
  </div>
  <button type="submit" class="btn btn-primary">{{ 'Update' if profile else 'Create' }}</button>
  <a href="/profiles" class="btn btn-outline-secondary">Cancel</a>
</form>
{% endblock %}
```

- [ ] **Step 4: Register profiles view router in main.py**

```python
from app.views import profiles as profile_views

app.include_router(profile_views.router)
```

- [ ] **Step 5: Commit**

```bash
git add web/app/views/profiles.py web/app/templates/profiles/ web/app/main.py
git commit -m "Add scan profiles pages with list, create, edit, and delete"
```

---

### Task 5: Schedules Pages (List + Create/Edit + Delete + Toggle)

**Files:**
- Create: `web/app/views/schedules.py`
- Create: `web/app/templates/schedules/list.html`
- Create: `web/app/templates/schedules/form.html`
- Modify: `web/app/main.py` (include router)

- [ ] **Step 1: Create the schedules view routes**

```python
# web/app/views/schedules.py
from fastapi import APIRouter, Request, Form
from fastapi.responses import HTMLResponse, RedirectResponse

from app.api.deps import DbSession
from app.models import Schedule, Asset, ScanProfile

router = APIRouter(prefix="/schedules", tags=["views"])


@router.get("/", response_class=HTMLResponse)
def list_schedules(request: Request, db: DbSession):
    from app.main import templates
    schedules = db.query(Schedule).order_by(Schedule.name).all()
    return templates.TemplateResponse("schedules/list.html", {"request": request, "schedules": schedules})


@router.get("/new", response_class=HTMLResponse)
def new_schedule(request: Request, db: DbSession):
    from app.main import templates
    assets = db.query(Asset).order_by(Asset.name).all()
    profiles = db.query(ScanProfile).order_by(ScanProfile.name).all()
    return templates.TemplateResponse(
        "schedules/form.html",
        {"request": request, "schedule": None, "assets": assets, "profiles": profiles},
    )


@router.get("/{schedule_id}/edit", response_class=HTMLResponse)
def edit_schedule(schedule_id: int, request: Request, db: DbSession):
    from app.main import templates
    schedule = db.get(Schedule, schedule_id)
    if not schedule:
        return RedirectResponse("/schedules", status_code=303)
    assets = db.query(Asset).order_by(Asset.name).all()
    profiles = db.query(ScanProfile).order_by(ScanProfile.name).all()
    return templates.TemplateResponse(
        "schedules/form.html",
        {"request": request, "schedule": schedule, "assets": assets, "profiles": profiles},
    )


@router.post("/", response_class=HTMLResponse)
def create_schedule(
    db: DbSession,
    name: str = Form(...),
    asset_id: int = Form(...),
    profile_id: int = Form(...),
    cron_expression: str = Form(...),
):
    schedule = Schedule(
        name=name, asset_id=asset_id, profile_id=profile_id, cron_expression=cron_expression
    )
    db.add(schedule)
    db.commit()
    return RedirectResponse("/schedules", status_code=303)


@router.post("/{schedule_id}", response_class=HTMLResponse)
def update_schedule(
    schedule_id: int,
    db: DbSession,
    name: str = Form(...),
    asset_id: int = Form(...),
    profile_id: int = Form(...),
    cron_expression: str = Form(...),
):
    schedule = db.get(Schedule, schedule_id)
    if not schedule:
        return RedirectResponse("/schedules", status_code=303)
    schedule.name = name
    schedule.asset_id = asset_id
    schedule.profile_id = profile_id
    schedule.cron_expression = cron_expression
    db.commit()
    return RedirectResponse("/schedules", status_code=303)


@router.post("/{schedule_id}/toggle")
def toggle_schedule(schedule_id: int, db: DbSession):
    schedule = db.get(Schedule, schedule_id)
    if schedule:
        schedule.enabled = not schedule.enabled
        db.commit()
        from app.main import templates
        # Return just the toggle button for HTMX swap
        state = "Disable" if schedule.enabled else "Enable"
        btn_class = "btn-outline-warning" if schedule.enabled else "btn-outline-success"
        return HTMLResponse(
            f'<button class="btn btn-sm {btn_class}" '
            f'hx-post="/schedules/{schedule_id}/toggle" '
            f'hx-swap="outerHTML">{state}</button>'
        )
    return HTMLResponse("")


@router.delete("/{schedule_id}")
def delete_schedule(schedule_id: int, db: DbSession):
    schedule = db.get(Schedule, schedule_id)
    if schedule:
        db.delete(schedule)
        db.commit()
    return HTMLResponse("")
```

- [ ] **Step 2: Create the schedules list template**

```html
{# web/app/templates/schedules/list.html #}
{% extends "base.html" %}
{% block title %}Schedules — nmapctf{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Schedules</h1>
  <a href="/schedules/new" class="btn btn-primary">Add Schedule</a>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Asset</th>
      <th>Profile</th>
      <th>Cron</th>
      <th>Status</th>
      <th>Last Run</th>
      <th style="width: 200px;">Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for sched in schedules %}
    <tr id="schedule-{{ sched.id }}">
      <td>{{ sched.name }}</td>
      <td>{{ sched.asset.name }}</td>
      <td>{{ sched.profile.name }}</td>
      <td><code>{{ sched.cron_expression }}</code></td>
      <td>
        {% if sched.enabled %}
        <span class="badge bg-success">Enabled</span>
        {% else %}
        <span class="badge bg-secondary">Disabled</span>
        {% endif %}
      </td>
      <td>{{ sched.last_run_at.strftime('%Y-%m-%d %H:%M') if sched.last_run_at else '—' }}</td>
      <td>
        <button class="btn btn-sm {{ 'btn-outline-warning' if sched.enabled else 'btn-outline-success' }}"
                hx-post="/schedules/{{ sched.id }}/toggle"
                hx-swap="outerHTML">{{ 'Disable' if sched.enabled else 'Enable' }}</button>
        <a href="/schedules/{{ sched.id }}/edit" class="btn btn-sm btn-outline-light">Edit</a>
        <button class="btn btn-sm btn-outline-danger"
                hx-delete="/schedules/{{ sched.id }}"
                hx-target="#schedule-{{ sched.id }}"
                hx-swap="outerHTML"
                hx-confirm="Delete schedule '{{ sched.name }}'?">Del</button>
      </td>
    </tr>
    {% else %}
    <tr><td colspan="7" class="text-muted text-center">No schedules yet. <a href="/schedules/new">Create one.</a></td></tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 3: Create the schedule form template**

```html
{# web/app/templates/schedules/form.html #}
{% extends "base.html" %}
{% block title %}{{ 'Edit' if schedule else 'New' }} Schedule — nmapctf{% endblock %}
{% block content %}
<h1>{{ 'Edit' if schedule else 'New' }} Schedule</h1>

<form method="post" action="{{ '/schedules/' ~ schedule.id if schedule else '/schedules/' }}" class="mt-3" style="max-width: 600px;">
  <div class="mb-3">
    <label for="name" class="form-label">Name</label>
    <input type="text" class="form-control" id="name" name="name" required
           value="{{ schedule.name if schedule else '' }}">
  </div>
  <div class="mb-3">
    <label for="asset_id" class="form-label">Asset</label>
    <select class="form-select" id="asset_id" name="asset_id" required>
      <option value="">Select asset...</option>
      {% for asset in assets %}
      <option value="{{ asset.id }}" {{ 'selected' if schedule and schedule.asset_id == asset.id else '' }}>
        {{ asset.name }} ({{ asset.address }})
      </option>
      {% endfor %}
    </select>
  </div>
  <div class="mb-3">
    <label for="profile_id" class="form-label">Scan Profile</label>
    <select class="form-select" id="profile_id" name="profile_id" required>
      <option value="">Select profile...</option>
      {% for profile in profiles %}
      <option value="{{ profile.id }}" {{ 'selected' if schedule and schedule.profile_id == profile.id else '' }}>
        {{ profile.name }}
      </option>
      {% endfor %}
    </select>
  </div>
  <div class="mb-3">
    <label for="cron_expression" class="form-label">Cron Expression</label>
    <input type="text" class="form-control" id="cron_expression" name="cron_expression" required
           placeholder="0 */6 * * *"
           value="{{ schedule.cron_expression if schedule else '' }}">
    <div class="form-text">Standard 5-field cron format. Example: <code>0 */6 * * *</code> = every 6 hours.</div>
  </div>
  <button type="submit" class="btn btn-primary">{{ 'Update' if schedule else 'Create' }}</button>
  <a href="/schedules" class="btn btn-outline-secondary">Cancel</a>
</form>
{% endblock %}
```

- [ ] **Step 4: Register schedules view router in main.py**

```python
from app.views import schedules as schedule_views

app.include_router(schedule_views.router)
```

- [ ] **Step 5: Commit**

```bash
git add web/app/views/schedules.py web/app/templates/schedules/ web/app/main.py
git commit -m "Add schedules pages with list, create, edit, delete, and toggle"
```

---

### Task 6: Scan Pages (Run Scan + History + Detail + Status Polling)

**Files:**
- Create: `web/app/views/scans.py`
- Create: `web/app/templates/scans/run.html`
- Create: `web/app/templates/scans/history.html`
- Create: `web/app/templates/scans/detail.html`
- Create: `web/app/templates/partials/scan_status.html`
- Modify: `web/app/main.py` (include router)

- [ ] **Step 1: Create the scans view routes**

```python
# web/app/views/scans.py
from fastapi import APIRouter, Request, Form
from fastapi.responses import HTMLResponse, RedirectResponse
from redis import Redis
from rq import Queue

from app.api.deps import DbSession
from app.config import settings
from app.models import ScanJob, ScanResult, Asset, ScanProfile, ScanStatus

router = APIRouter(prefix="/scans", tags=["views"])


def _get_queue() -> Queue:
    conn = Redis.from_url(settings.redis_url)
    return Queue("scans", connection=conn)


@router.get("/", response_class=HTMLResponse)
def scan_history(
    request: Request,
    db: DbSession,
    status: str | None = None,
    asset_id: int | None = None,
):
    from app.main import templates

    query = db.query(ScanJob)
    if status:
        query = query.filter(ScanJob.status == status)
    if asset_id:
        query = query.filter(ScanJob.asset_id == asset_id)
    scans = query.order_by(ScanJob.queued_at.desc()).limit(100).all()

    assets = db.query(Asset).order_by(Asset.name).all()
    statuses = [s.value for s in ScanStatus]

    return templates.TemplateResponse(
        "scans/history.html",
        {
            "request": request,
            "scans": scans,
            "assets": assets,
            "statuses": statuses,
            "filter_status": status,
            "filter_asset_id": asset_id,
        },
    )


@router.get("/run", response_class=HTMLResponse)
def run_scan_form(request: Request, db: DbSession):
    from app.main import templates
    assets = db.query(Asset).order_by(Asset.name).all()
    profiles = db.query(ScanProfile).order_by(ScanProfile.name).all()
    return templates.TemplateResponse(
        "scans/run.html",
        {"request": request, "assets": assets, "profiles": profiles},
    )


@router.post("/run", response_class=HTMLResponse)
def run_scan(
    db: DbSession,
    asset_id: int = Form(...),
    profile_id: int = Form(...),
):
    asset = db.get(Asset, asset_id)
    profile = db.get(ScanProfile, profile_id)
    if not asset or not profile:
        return RedirectResponse("/scans/run", status_code=303)

    job = ScanJob(asset_id=asset_id, profile_id=profile_id)
    db.add(job)
    db.commit()
    db.refresh(job)

    queue = _get_queue()
    queue.enqueue(
        "tasks.run_scan",
        job_id=job.id,
        target=asset.address,
        nmap_args=profile.nmap_args,
        job_timeout="30m",
    )

    return RedirectResponse(f"/scans/{job.id}", status_code=303)


@router.get("/{scan_id}", response_class=HTMLResponse)
def scan_detail(scan_id: str, request: Request, db: DbSession):
    from app.main import templates
    scan = db.get(ScanJob, scan_id)
    if not scan:
        return RedirectResponse("/scans", status_code=303)
    return templates.TemplateResponse(
        "scans/detail.html",
        {"request": request, "scan": scan},
    )


@router.get("/{scan_id}/status", response_class=HTMLResponse)
def scan_status_partial(scan_id: str, db: DbSession, request: Request):
    """HTMX endpoint for polling scan status."""
    from app.main import templates
    scan = db.get(ScanJob, scan_id)
    if not scan:
        return HTMLResponse("")
    return templates.TemplateResponse(
        "partials/scan_status.html",
        {"request": request, "scan": scan},
    )
```

- [ ] **Step 2: Create the run scan template**

```html
{# web/app/templates/scans/run.html #}
{% extends "base.html" %}
{% block title %}Run Scan — nmapctf{% endblock %}
{% block content %}
<h1>Run Scan</h1>

<form method="post" action="/scans/run" class="mt-3" style="max-width: 600px;">
  <div class="mb-3">
    <label for="asset_id" class="form-label">Target Asset</label>
    <select class="form-select" id="asset_id" name="asset_id" required>
      <option value="">Select asset...</option>
      {% for asset in assets %}
      <option value="{{ asset.id }}">{{ asset.name }} ({{ asset.address }})</option>
      {% endfor %}
    </select>
  </div>
  <div class="mb-3">
    <label for="profile_id" class="form-label">Scan Profile</label>
    <select class="form-select" id="profile_id" name="profile_id" required>
      <option value="">Select profile...</option>
      {% for profile in profiles %}
      <option value="{{ profile.id }}">{{ profile.name }} — {{ profile.nmap_args }}</option>
      {% endfor %}
    </select>
  </div>
  <button type="submit" class="btn btn-primary">Start Scan</button>
</form>
{% endblock %}
```

- [ ] **Step 3: Create the scan history template**

```html
{# web/app/templates/scans/history.html #}
{% extends "base.html" %}
{% block title %}Scan History — nmapctf{% endblock %}
{% block content %}
<h1 class="mb-4">Scan History</h1>

<form method="get" action="/scans" class="row g-2 mb-3">
  <div class="col-auto">
    <select class="form-select form-select-sm" name="status">
      <option value="">All statuses</option>
      {% for s in statuses %}
      <option value="{{ s }}" {{ 'selected' if filter_status == s else '' }}>{{ s }}</option>
      {% endfor %}
    </select>
  </div>
  <div class="col-auto">
    <select class="form-select form-select-sm" name="asset_id">
      <option value="">All assets</option>
      {% for asset in assets %}
      <option value="{{ asset.id }}" {{ 'selected' if filter_asset_id == asset.id else '' }}>{{ asset.name }}</option>
      {% endfor %}
    </select>
  </div>
  <div class="col-auto">
    <button type="submit" class="btn btn-sm btn-outline-light">Filter</button>
  </div>
</form>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Asset</th>
      <th>Profile</th>
      <th>Status</th>
      <th>Trigger</th>
      <th>Queued</th>
      <th>Completed</th>
      <th>Results</th>
    </tr>
  </thead>
  <tbody>
    {% for scan in scans %}
    <tr>
      <td>{{ scan.asset.name }}</td>
      <td>{{ scan.profile.name }}</td>
      <td><span class="scan-status-{{ scan.status.value }}">{{ scan.status.value }}</span></td>
      <td>{{ scan.trigger.value }}</td>
      <td>{{ scan.queued_at.strftime('%Y-%m-%d %H:%M') }}</td>
      <td>{{ scan.completed_at.strftime('%Y-%m-%d %H:%M') if scan.completed_at else '—' }}</td>
      <td><a href="/scans/{{ scan.id }}" class="btn btn-sm btn-outline-light">View</a></td>
    </tr>
    {% else %}
    <tr><td colspan="7" class="text-muted text-center">No scans found.</td></tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
```

- [ ] **Step 4: Create the scan detail template**

```html
{# web/app/templates/scans/detail.html #}
{% extends "base.html" %}
{% block title %}Scan Detail — nmapctf{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Scan Detail</h1>
  <a href="/scans" class="btn btn-outline-secondary">Back to History</a>
</div>

<div class="card mb-4">
  <div class="card-body">
    <div class="row">
      <div class="col-md-3"><strong>Asset:</strong> {{ scan.asset.name }} ({{ scan.asset.address }})</div>
      <div class="col-md-3"><strong>Profile:</strong> {{ scan.profile.name }}</div>
      <div class="col-md-3">
        <strong>Status:</strong>
        <span id="scan-status-badge"
              hx-get="/scans/{{ scan.id }}/status"
              hx-trigger="{{ 'every 3s' if scan.status.value in ('pending', 'running') else 'none' }}"
              hx-swap="outerHTML">
          {% include "partials/scan_status.html" %}
        </span>
      </div>
      <div class="col-md-3"><strong>Trigger:</strong> {{ scan.trigger.value }}</div>
    </div>
    <div class="row mt-2">
      <div class="col-md-3"><strong>Queued:</strong> {{ scan.queued_at.strftime('%Y-%m-%d %H:%M:%S') }}</div>
      <div class="col-md-3"><strong>Started:</strong> {{ scan.started_at.strftime('%Y-%m-%d %H:%M:%S') if scan.started_at else '—' }}</div>
      <div class="col-md-3"><strong>Completed:</strong> {{ scan.completed_at.strftime('%Y-%m-%d %H:%M:%S') if scan.completed_at else '—' }}</div>
    </div>
    {% if scan.error_message %}
    <div class="alert alert-danger mt-3 mb-0">{{ scan.error_message }}</div>
    {% endif %}
  </div>
</div>

{% if scan.results %}
<div class="card mb-4">
  <div class="card-header">Results ({{ scan.results | length }} ports found)</div>
  <div class="card-body p-0">
    <table class="table table-hover mb-0">
      <thead>
        <tr>
          <th>Host</th>
          <th>Port</th>
          <th>Protocol</th>
          <th>State</th>
          <th>Service</th>
          <th>Version</th>
        </tr>
      </thead>
      <tbody>
        {% for result in scan.results %}
        <tr>
          <td>{{ result.host }}</td>
          <td>{{ result.port }}</td>
          <td>{{ result.protocol }}</td>
          <td>
            {% if result.state == 'open' %}
            <span class="badge bg-success">open</span>
            {% elif result.state == 'filtered' %}
            <span class="badge bg-warning">filtered</span>
            {% else %}
            <span class="badge bg-secondary">{{ result.state }}</span>
            {% endif %}
          </td>
          <td>{{ result.service or '—' }}</td>
          <td>{{ result.version or '—' }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endif %}

{% if scan.raw_xml %}
<div class="card">
  <div class="card-header">
    Raw Nmap Output
    <button class="btn btn-sm btn-outline-light float-end" type="button" data-bs-toggle="collapse" data-bs-target="#rawXml">
      Toggle
    </button>
  </div>
  <div class="collapse" id="rawXml">
    <div class="card-body">
      <pre class="raw-xml"><code>{{ scan.raw_xml }}</code></pre>
    </div>
  </div>
</div>
{% endif %}
{% endblock %}
```

- [ ] **Step 5: Create the scan status partial for HTMX polling**

```html
{# web/app/templates/partials/scan_status.html #}
<span id="scan-status-badge"
      class="scan-status-{{ scan.status.value }}"
      {% if scan.status.value in ('pending', 'running') %}
      hx-get="/scans/{{ scan.id }}/status"
      hx-trigger="every 3s"
      hx-swap="outerHTML"
      {% endif %}>
  {{ scan.status.value }}
</span>
```

- [ ] **Step 6: Register scans view router in main.py**

```python
from app.views import scans as scan_views

app.include_router(scan_views.router)
```

- [ ] **Step 7: Commit**

```bash
git add web/app/views/scans.py web/app/templates/scans/ web/app/templates/partials/scan_status.html web/app/main.py
git commit -m "Add scan pages with run, history, detail, and HTMX status polling"
```

---

### Task 7: Functional Tests

**Files:**
- Create: `tests/web/test_views.py`

- [ ] **Step 1: Create functional tests for all view routes**

```python
# tests/web/test_views.py
"""Functional tests for web UI view routes."""
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from app.database import Base, get_db
from app.main import app
from app.models import Asset, AssetType, ScanProfile, ScanJob, Schedule


@pytest.fixture
def db_session():
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    Base.metadata.create_all(bind=engine)
    Session = sessionmaker(bind=engine)
    session = Session()
    yield session
    session.close()


@pytest.fixture
def client(db_session):
    def override_get_db():
        yield db_session

    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()


@pytest.fixture
def sample_asset(db_session):
    asset = Asset(name="test-server", type=AssetType.ip, address="192.168.1.1")
    db_session.add(asset)
    db_session.commit()
    db_session.refresh(asset)
    return asset


@pytest.fixture
def sample_profile(db_session):
    profile = ScanProfile(name="Quick Scan", nmap_args="-T4 -F", is_default=True)
    db_session.add(profile)
    db_session.commit()
    db_session.refresh(profile)
    return profile


class TestDashboard:
    def test_dashboard_loads(self, client):
        resp = client.get("/")
        assert resp.status_code == 200
        assert "Dashboard" in resp.text

    def test_dashboard_shows_stats(self, client, sample_asset):
        resp = client.get("/")
        assert resp.status_code == 200
        assert "Assets" in resp.text


class TestAssetViews:
    def test_list_assets(self, client, sample_asset):
        resp = client.get("/assets")
        assert resp.status_code == 200
        assert "test-server" in resp.text

    def test_new_asset_form(self, client):
        resp = client.get("/assets/new")
        assert resp.status_code == 200
        assert "New" in resp.text

    def test_create_asset(self, client):
        resp = client.post(
            "/assets/",
            data={"name": "web-host", "type": "host", "address": "example.com", "notes": ""},
            follow_redirects=False,
        )
        assert resp.status_code == 303

    def test_edit_asset_form(self, client, sample_asset):
        resp = client.get(f"/assets/{sample_asset.id}/edit")
        assert resp.status_code == 200
        assert "192.168.1.1" in resp.text

    def test_delete_asset(self, client, sample_asset):
        resp = client.delete(f"/assets/{sample_asset.id}")
        assert resp.status_code == 200


class TestProfileViews:
    def test_list_profiles(self, client, sample_profile):
        resp = client.get("/profiles")
        assert resp.status_code == 200
        assert "Quick Scan" in resp.text

    def test_new_profile_form(self, client):
        resp = client.get("/profiles/new")
        assert resp.status_code == 200


class TestScheduleViews:
    def test_list_schedules(self, client):
        resp = client.get("/schedules")
        assert resp.status_code == 200
        assert "Schedules" in resp.text

    def test_new_schedule_form(self, client):
        resp = client.get("/schedules/new")
        assert resp.status_code == 200


class TestScanViews:
    def test_scan_history(self, client):
        resp = client.get("/scans")
        assert resp.status_code == 200
        assert "Scan History" in resp.text

    def test_run_scan_form(self, client):
        resp = client.get("/scans/run")
        assert resp.status_code == 200
        assert "Run Scan" in resp.text
```

- [ ] **Step 2: Run tests**

Run: `cd /home/whiskey/nmapctf && python -m pytest tests/web/test_views.py -v`
Expected: All tests PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/web/test_views.py
git commit -m "Add functional tests for web UI views"
```
